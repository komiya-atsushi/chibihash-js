{
  "name": "chibihash",
  "version": "1.0.0",
  "scripts": {
    "test": "npm --workspaces --if-present run test",
    "lint": "npm --workspaces --if-present run lint",
    "clean": "npm --workspaces --if-present run clean",
    "fix": "npm --workspaces --if-present run fix"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.7.5",
    "gts": "^6.0.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  },
  "workspaces": [
    "packages/chibihash-js"
  ]
}
